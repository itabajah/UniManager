/**
 * Google API Configuration Template
 * 
 * SETUP INSTRUCTIONS:
 * 1. Copy this file to: js/google-config.js
 * 2. Replace the placeholder values below with your actual Google Cloud credentials
 * 3. Follow the setup guide in the README to create your credentials
 * 
 * SECURITY NOTE:
 * - The Client ID is safe to expose (it's public in client-side apps)
 * - The API Key should be restricted in Google Cloud Console to your domains
 */

const GOOGLE_CONFIG = {
    CLIENT_ID: 'YOUR_CLIENT_ID_HERE.apps.googleusercontent.com',
    API_KEY: 'YOUR_API_KEY_HERE'
};
